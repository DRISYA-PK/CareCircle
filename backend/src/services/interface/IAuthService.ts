import { IUserModel } from '../../models/implements/user.model.js';

export interface IAuthService {
  // signUp(user: IUserModel): Promise<string>;
  // verifyOtp(
  //   otp: string,
  //   email: string,
  // ): Promise<{ user: IUserModel; accessToken: string; refreshToken: string }>;
}
